# My Smart AI Project
This is an AI-powered search engine built with Python and SAP HANA.

## Setup
1. Install dependencies using `pip install -r requirements.txt`
2. Run the server using `python main.py`